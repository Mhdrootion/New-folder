<Page
    x:Class="JamiaNadwiyya.Pages.cloudPage"
   
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:local="using:JamiaNadwiyya.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBox Name="messagetextbx" Margin="20" Height="40" PlaceholderText="Enter your message here" ></TextBox>
            <Button Name="sendButton" Content="Send" Margin="20" Width="50" Click="sendButton_Click"></Button>
          <ListView x:Name="ListItems">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                
                                    <TextBlock Name="showMEssage" Text="{Binding Text}" MaxWidth="550" HorizontalAlignment="Left" TextWrapping="WrapWholeWords"/>
                                
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
            
        </StackPanel>
    </Grid>
</Page>
